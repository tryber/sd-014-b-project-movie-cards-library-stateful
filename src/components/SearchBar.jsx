import React, { Component } from 'react';

class SearchBar extends Component {
  /* constructor() {
    super();

    this.state = {
      searchText: '',
    };
  } */

  render() {
    return (
      <MovieList />
    );
  }
}

export default SearchBar;
