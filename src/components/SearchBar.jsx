// implement SearchBar component here
import React from 'react';

class SearchBar extends React.Component {

  render() {

    const movies = { searchText, onSearchTextChange, bookmarkedOnly, onBookMarkedChange, selectedGenre, onSelectedGenreChange } = this.props;

    return ()
  }
}

export default SearchBar;
